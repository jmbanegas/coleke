import { Component } from '@angular/core';

@Component({
  selector: 'app-footer',
  standalone: true,
  imports: [
    // Aquí agregarías otros módulos o componentes si los necesitas
  ],
  templateUrl: './footer.component.html',
  styleUrls: ['./footer.component.css'] // Corregido a styleUrls y en formato de array
})
export class FooterComponent {
  // Aquí puedes agregar propiedades y métodos para tu componente
}
